import React from "react";

const Chains= () => {
  return <h1 style={styles.page}>Create an account to explore more! CHainss</h1>;
};

const styles = { page: { textAlign: "center", marginTop: "50px", fontSize: "24px", color: "purple" } };

export default Chains;