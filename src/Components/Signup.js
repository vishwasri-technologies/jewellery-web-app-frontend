import React from "react";

const Signup = () => {
  return <h1 style={styles.page}>Create an account to explore more! signupp</h1>;
};

const styles = { page: { textAlign: "center", marginTop: "50px", fontSize: "24px", color: "purple" } };

export default Signup;
